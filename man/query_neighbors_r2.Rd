% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locations.R
\name{query_neighbors_r2}
\alias{query_neighbors_r2}
\title{Radius neighbour query on event centroids}
\usage{
query_neighbors_r2(
  coords,
  r2,
  distance_metric = c("haversine", "euclidean"),
  weighted = FALSE
)
}
\arguments{
\item{coords}{matrix `n x 2` with columns `longitude`, `latitude`.}

\item{r2}{radius. Metres for `"haversine"`, raw units for `"euclidean"`.}

\item{distance_metric}{`"haversine"` or `"euclidean"`.}

\item{weighted}{if `TRUE`, also return per-edge distances (for the
weighted Infomap formula).}
}
\value{
A list with components
  * `neighbors`: list of integer vectors (1-based; includes the node
    itself).
  * `distances`: list of numeric vectors of matching length, or
    `NULL` when `weighted = FALSE`.
}
\description{
Mirrors `infostop.utils.query_neighbors`. Brute-force pairwise (O(N^2));
fine for the centroid counts the pipeline produces.
}
\keyword{internal}
