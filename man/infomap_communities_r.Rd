% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locations.R
\name{infomap_communities_r}
\alias{infomap_communities_r}
\title{Two-level Infomap community detection on the centroid network}
\usage{
infomap_communities_r(
  neighbors,
  distances = NULL,
  counts,
  weight_exponent = 1,
  label_singleton = TRUE,
  nb_trials = 1L
)
}
\arguments{
\item{neighbors, distances}{list output from [query_neighbors_r2()].
Pass `distances = NULL` for unweighted edges.}

\item{counts}{per-node counts.}

\item{weight_exponent}{numeric scalar.}

\item{label_singleton}{logical; see description.}

\item{nb_trials}{Infomap `--num-trials` value. The default `1` matches
the Python reference.}
}
\value{
Integer vector of labels of length `length(neighbors)`.
}
\description{
Mirrors `infostop.utils.infomap_communities` + `label_network`. Returns
a per-node integer label. Singletons (nodes with no neighbours within
`r2`) are either given a fresh label per node (`label_singleton = TRUE`)
or marked as `-1` (`label_singleton = FALSE`).
}
\details{
Uses the mapequation `infomap` R package. For Python-reference
compatibility, the network is passed through Infomap's link-list parser
with compact 0-based node ids, a fixed seed, and stable weight formatting.
}
\keyword{internal}
