% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_infomap.R
\name{spatial_infomap_xyt}
\alias{spatial_infomap_xyt}
\alias{spatial_infomap}
\alias{spatial_infomap.matrix}
\alias{spatial_infomap.data.frame}
\alias{spatial_infomap.trackframe}
\alias{spatial_infomap.move2}
\alias{spatial_infomap.sftrack}
\title{Spatial Infomap Cluster a Collection of Points Using Infomap}
\usage{
spatial_infomap_xyt(
  easting,
  northing,
  r2 = 10,
  label_singleton = TRUE,
  min_spacial_resolution = 0,
  distance_metric = c("haversine", "euclidean"),
  weighted = FALSE,
  weight_exponent = 1,
  verbose = FALSE
)

spatial_infomap(
  data,
  r2 = 10,
  label_singleton = TRUE,
  min_spacial_resolution = 0,
  distance_metric = c("haversine", "euclidean"),
  weighted = FALSE,
  weight_exponent = 1,
  verbose = FALSE,
  ...
)

\method{spatial_infomap}{matrix}(
  data,
  r2 = 10,
  label_singleton = TRUE,
  min_spacial_resolution = 0,
  distance_metric = c("haversine", "euclidean"),
  weighted = FALSE,
  weight_exponent = 1,
  verbose = FALSE,
  ...
)

\method{spatial_infomap}{data.frame}(
  data,
  r2 = 10,
  label_singleton = TRUE,
  min_spacial_resolution = 0,
  distance_metric = c("haversine", "euclidean"),
  weighted = FALSE,
  weight_exponent = 1,
  verbose = FALSE,
  ...
)

\method{spatial_infomap}{trackframe}(
  data,
  r2 = 10,
  label_singleton = TRUE,
  min_spacial_resolution = 0,
  distance_metric = "euclidean",
  weighted = FALSE,
  weight_exponent = 1,
  verbose = FALSE,
  ...
)

\method{spatial_infomap}{move2}(
  data,
  r2 = 10,
  label_singleton = TRUE,
  min_spacial_resolution = 0,
  distance_metric = c("haversine", "euclidean"),
  weighted = FALSE,
  weight_exponent = 1,
  verbose = FALSE,
  ...
)

\method{spatial_infomap}{sftrack}(
  data,
  r2 = 10,
  label_singleton = TRUE,
  min_spacial_resolution = 0,
  distance_metric = c("haversine", "euclidean"),
  weighted = FALSE,
  weight_exponent = 1,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{easting}{a numeric vector of x-coordinates (easting).}

\item{northing}{a numeric vector of y-coordinates (northing).}

\item{r2}{Numeric. Max distance between stationary points to form an edge.}

\item{label_singleton}{Logical. If TRUE, give stationary locations that were only visited once their own label.
If FALSE, label them as non-stationary (-1).}

\item{min_spacial_resolution}{Numeric. The minimal difference allowed between points before they are considered the same points.}

\item{distance_metric}{Character. Either 'haversine' (for geo data) or 'euclidean'.}

\item{weighted}{Logical. Weight edges in the network representation by distance.}

\item{weight_exponent}{Numeric. Exponent used when weighting edges in the network.}

\item{verbose}{Logical. Print output during the fitting procedure.}

\item{data}{A numeric matrix with 2 or 3 columns. Columns 1 and 2 are spatial coordinates.
Column 3 is optional and represents time.}

\item{...}{other arguments passed to `as.trackframe()`}
}
\value{
A numeric vector of cluster labels for each input point. Points labeled -1 are considered non-stationary.

A numeric vector of cluster labels for each input point. Points labeled -1 are considered non-stationary.
}
\description{
This function applies the SpatialInfomap algorithm to cluster a collection of points.
It directly returns the cluster labels rather than a model object.

This function applies the SpatialInfomap algorithm to cluster a collection of points.
It directly returns the cluster labels rather than a model object.
}
\examples{
if (is_infostop_initialized()) {
data <- infostop:::example_data()
stops <- find_stops(data, r1 = 100, min_staying_time = 300,
                    max_time_between = 86400, min_size = 2,
                    distance_metric = "haversine")
clusters <- spatial_infomap(stops$stop_events, r2 = 50)
labels <- match_labels(clusters, stops$event_map)
}
if (is_infostop_initialized()) {
data <- infostop:::example_data()
stops <- find_stops(data, r1 = 100, min_staying_time = 300,
                    max_time_between = 86400, min_size = 2,
                    distance_metric = "haversine")
clusters <- spatial_infomap(stops$stop_events, r2 = 50)
labels <- match_labels(clusters, stops$event_map)
}
}
