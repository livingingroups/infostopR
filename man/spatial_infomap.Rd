% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{spatial_infomap}
\alias{spatial_infomap}
\title{Spatial Infomap Cluster a Collection of Points Using Infomap}
\usage{
spatial_infomap(
  data,
  r2 = 10,
  label_singleton = TRUE,
  min_spacial_resolution = 0,
  distance_metric = c("haversine", "euclidean"),
  weighted = FALSE,
  weight_exponent = 1,
  verbose = FALSE
)
}
\arguments{
\item{data}{A numeric matrix with 2 or 3 columns. Columns 1 and 2 are spatial coordinates.
Column 3 is optional and represents time.}

\item{r2}{Numeric. Max distance between stationary points to form an edge.}

\item{label_singleton}{Logical. If TRUE, give stationary locations that were only visited once their own label.
If FALSE, label them as non-stationary (-1).}

\item{min_spacial_resolution}{Numeric. The minimal difference allowed between points before they are considered the same points.}

\item{distance_metric}{Character. Either 'haversine' (for geo data) or 'euclidean'.}

\item{weighted}{Logical. Weight edges in the network representation by distance.}

\item{weight_exponent}{Numeric. Exponent used when weighting edges in the network.}

\item{verbose}{Logical. Print output during the fitting procedure.}
}
\value{
A numeric vector of cluster labels for each input point. Points labeled -1 are considered non-stationary.
}
\description{
This function applies the SpatialInfomap algorithm to cluster a collection of points.
It directly returns the cluster labels rather than a model object.
}
\examples{
if (is_infostop_initialized()) {
data <- matrix(c(
  55.75259295, 12.34353885,
  55.7525908, 12.34353145,
  63.40379175, 10.40477095
), ncol = 2, byrow = TRUE)

labels <- spatial_infomap(data, r2 = 15)

unique(labels)
}
}
